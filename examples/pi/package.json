{
  "name": "Thr0w API Pi",
  "version": "0.0.1",
  "description": "Example that sends messages to channel 0 on changes to two pins.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL",
  "dependencies": {
    "rpio": "^0.9.11",
    "thr0w-api": "0.0.1"
  }
}
